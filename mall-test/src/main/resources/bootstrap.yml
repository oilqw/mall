spring:
  cloud:
    config:
      name: config-client
      label: master
      profile: dev
      username: admin
      password: admin123
      discovery:
        enabled: true
        service-id: configserver
management:
  security:
    enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://ouwenjie.com:7061/eureka