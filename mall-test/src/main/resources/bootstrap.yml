spring:
  application:
    name: mall-test
  cloud:
    config:
      enabled: true
      name: config-client
      label: master
      profile: dev
      #uri: http://ouwenjie.com:7062 
      username: admin
      password: admin123
      discovery:
        enabled: true
        serviceId: configServer
management:
  security:
    enabled: true