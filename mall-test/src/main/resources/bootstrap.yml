spring:
  application:
    name: mall-test
  cloud:
    config:
      name: config-client
      label: master
      profile: dev
      uri: http://ouwenjie.com:7062 
      username: admin
      password: admin123
   #   discovery:
   #     enabled: true
     #   serviceId: config_server
eureka:
  client:
    serviceUrl:
      defaultZone: http://ouwenjie.com:7061/eureka/