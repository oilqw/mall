spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config_server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/