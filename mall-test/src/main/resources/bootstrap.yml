spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config_server
eureka:
  client:
    service-url:
      defaultZone: http://ouwenjie.com:7061/eureka/